# Secrets — never commit
.env
.env.*
!.env.example
.env.bak*
*.snapshot
env.snapshot
*.pem
*.key
id_rsa*
secrets/
**/*CREDENTIALS*
**/*credentials*

# DB dumps
*.sql
*.sql.gz
*.dump
# ...but migrations are source, not dumps. Without this the `*.sql` rule above
# silently excluded every db/migrations/*.sql from the repo — the schema existed
# only on the live box and in backups, so a checkout could never rebuild the DB.
!db/migrations/*.sql

# Backups & runtime
backups/
logs/
*.log

# Node
node_modules/
.next/
dist/
build/
coverage/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Docker
docker-compose.override.yml

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp
reports/private/

# Postal runtime config (contains signing keys / DB creds)
postal/config/
postal/data/
postal/**/postal.yml
